name: Deploy to K8s

on:
  push:
    branches: [main, master]
    paths-ignore:
      - 'backstage-app/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/argocd-sync.yaml
    with:
      application: k8s-idp
    secrets: inherit

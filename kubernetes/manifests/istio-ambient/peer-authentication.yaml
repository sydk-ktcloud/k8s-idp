apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: trip-app
spec:
  # NodePort(30249 등)로 들어오는 외부 트래픽은 plain HTTP이므로 PERMISSIVE 사용.
  # 네임스페이스 내 pod-to-pod 통신은 ambient mesh에 속해 있어
  # ztunnel이 자동으로 HBONE mTLS를 적용.
  mtls:
    mode: PERMISSIVE

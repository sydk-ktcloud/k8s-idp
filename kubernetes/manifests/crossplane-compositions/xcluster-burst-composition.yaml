apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xclusterburst.gcp.k8s-idp.example.org
  labels:
    provider: gcp
    resource: container
    type: burst
spec:
  compositeTypeRef:
    apiVersion: k8s-idp.example.org/v1alpha1
    kind: XClusterBurst
  writeConnectionSecretsToNamespace: crossplane-system
  resources:
    - name: gke-burst-cluster
      base:
        apiVersion: container.gcp.upbound.io/v1beta1
        kind: Cluster
        spec:
          forProvider:
            labels:
              environment: burst
              managed-by: crossplane
              cost-center: burst-workload
              preemptible: "true"
            location: asia-northeast3
            network: projects/sydk-ktcloud/global/networks/default
            subnetwork: projects/sydk-ktcloud/regions/asia-northeast3/subnetworks/default
            initialNodeCount: 1
            enableIntranodeVisibility: true
            nodeConfig:
              - machineType: e2-standard-2
                diskSizeGb: 50
                preemptible: true
                oauthScopes:
                  - "https://www.googleapis.com/auth/cloud-platform"
                labels:
                  environment: burst
                  preemptible: "true"
          writeConnectionSecretToRef:
            namespace: crossplane-system
            name: placeholder
          providerConfigRef:
            name: default
      patches:
        - fromFieldPath: spec.name
          toFieldPath: metadata.annotations[crossplane.io/external-name]
        - fromFieldPath: spec.location
          toFieldPath: spec.forProvider.location
        - fromFieldPath: spec.machineType
          toFieldPath: spec.forProvider.nodeConfig[0].machineType
        - fromFieldPath: spec.diskSizeGb
          toFieldPath: spec.forProvider.nodeConfig[0].diskSizeGb
        - fromFieldPath: spec.preemptible
          toFieldPath: spec.forProvider.nodeConfig[0].preemptible
        - fromFieldPath: metadata.uid
          toFieldPath: spec.writeConnectionSecretToRef.name
          transforms:
            - type: string
              string:
                fmt: "%s-gke-cluster-conn"
      connectionDetails:
        - name: kubeconfig
          fromConnectionSecretKey: kubeconfig

    - name: gke-burst-nodepool
      base:
        apiVersion: container.gcp.upbound.io/v1beta1
        kind: NodePool
        spec:
          forProvider:
            clusterSelector:
              matchControllerRef: true
            location: asia-northeast3
            initialNodeCount: 1
            autoscaling:
              - minNodeCount: 0
                maxNodeCount: 5
            nodeConfig:
              - machineType: e2-standard-2
                diskSizeGb: 50
                preemptible: true
                oauthScopes:
                  - "https://www.googleapis.com/auth/cloud-platform"
                labels:
                  environment: burst
                  preemptible: "true"
            management:
              - autoRepair: true
                autoUpgrade: true
          providerConfigRef:
            name: default
      patches:
        - fromFieldPath: spec.location
          toFieldPath: spec.forProvider.location
        - fromFieldPath: spec.machineType
          toFieldPath: spec.forProvider.nodeConfig[0].machineType
        - fromFieldPath: spec.diskSizeGb
          toFieldPath: spec.forProvider.nodeConfig[0].diskSizeGb
        - fromFieldPath: spec.preemptible
          toFieldPath: spec.forProvider.nodeConfig[0].preemptible
        - fromFieldPath: spec.minNodeCount
          toFieldPath: spec.forProvider.autoscaling[0].minNodeCount
        - fromFieldPath: spec.maxNodeCount
          toFieldPath: spec.forProvider.autoscaling[0].maxNodeCount
